export default class UserModel {
    constructor(id, accountNumber, password, accountType, accountName) {
      this.id = id;
      this.accountNumber = accountNumber;
      this.password = password;
      this.accountType = accountType;
      this.accountName = accountName;
    }

    //Sample model; to be expanded in future
    // Additional methods can be added here for data handling
  }
  